#pragma once
#include "Tcore.h"
#include "TWrite.h"

class Sample : public TCore
{
public:
	TWrite		m_Font;
	
public:
	bool		Init();
	bool		Frame();
	bool		Render();
	bool		Release();
	bool   ResizeClient(UINT iWidth, UINT iHeight);
	Sample();
	virtual ~Sample();
};

